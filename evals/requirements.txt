# Behavioral eval harness (evals/run_evals.py) — runs the real agent against a board.
# Needs the `claude` CLI (agent + keyless judge) + a board token; ANTHROPIC_API_KEY for CI.
pyyaml>=6
deepeval>=4
