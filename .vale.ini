StylesPath = styles
MinAlertLevel = error

# Shipped skills/** files are the installed product surface — instruction-only.
# Only this tree is in scope; tests/, evals/ and root docs are excluded.
[skills/**]
Squad.InstructionOnly = error
